provider: DATA_PROVIDER_REST
method: GET
url: https://api.pro.coinbase.com/products/{symbol}/ticker
parameters:
  symbol:
    location: path
    required: true
    type: string
    value: BTC-USD
# outputTransform: "$.{ \"dt\": time, \"ms\": $toMillis(time), \"price\" : $formatNumber($number(price), '#.00000000') }"
