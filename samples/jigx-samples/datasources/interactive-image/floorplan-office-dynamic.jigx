type: 'datasource.sqlite'
options:
  provider: DATA_PROVIDER_DYNAMIC

  entities:
    - entity: default/floorplan-office-dynamic

  query: SELECT id, '$.name', '$.xCoordinate', '$.yCoordinate', '$.isReserved' FROM [default/floorplan-office-dynamic]
