title: Employees list
type: jig.list
description: In this example we have action list on onPress function in list-item. First action will sync our entity with database and the second action will redirect us to detail of the employe which we clicked it.


data: =@ctx.datasources.employees-dynamic
item: 
  type: "component.list-item"
  options:
    title: =@ctx.current.item.firstname
    subtitle: =@ctx.current.item.email
    rightElement: 
      element: switch
      onChange: 
        type: action.action-list
        options:
          actions:
            - type: action.sync-entities
              options:
                provider: DATA_PROVIDER_DYNAMIC
                entities:
                  - entity: default/employee-detail
            - type: action.go-to
              options:
                linkTo: employee-detail-dynamic
                parameters:
                  id: =@ctx.current.item.id